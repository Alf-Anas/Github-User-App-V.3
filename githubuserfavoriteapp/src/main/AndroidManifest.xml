<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.lofrus.githubuserfavoriteapp">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="com.lofrus.githubuserappv3.READ_DATABASE"/>
    <uses-permission android:name="com.lofrus.githubuserappv3.WRITE_DATABASE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.GithubUserAppV3"
        tools:ignore="AllowBackup">
        <activity android:name=".MainFavActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".DetailUserActivity"
            android:configChanges="orientation|keyboardHidden"
            android:label="@string/app_name_detail"
            android:screenOrientation="portrait" />
    </application>

    <queries>
        <package android:name="com.lofrus.githubuserappv3" />
    </queries>

</manifest>